install:
  - sudo apt-get update
  - sudo apt-get install debootstrap
  - cd /
  - mkdir /wheezy-chroot
main # debootstrap wheezy ./wheezy-chroot http://http.debian.net/debian/
  - ls 
script:
  - echo hello world
